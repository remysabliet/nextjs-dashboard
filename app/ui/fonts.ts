import { Inter, Lusitana, Nunito } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({ subsets: ['latin'], weight: '400' });

export const nunito = Nunito({ subsets: ['latin'], weight: '200' });
